const defaultState = {
  isCreating: false,
  topics: [],
  create: {
    stage: 0,
  },
};
function topics(state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default topics;
